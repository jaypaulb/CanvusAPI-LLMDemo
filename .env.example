# CanvusLocalLLM Configuration
# Copy this file to .env and fill in your values

# ======================
# Canvus Server Settings (Required)
# ======================

# URL of your Canvus server (include trailing slash)
CANVUS_SERVER=https://your-canvus-server.example.com/

# API key for Canvus authentication
CANVUS_API_KEY=your-canvus-api-key-here

# Canvus username (email address)
CANVUS_USERNAME=your-email@example.com

# Canvus password
CANVUS_PASSWORD=your-password-here

# ID of the canvas to monitor (found in canvas URL or settings)
CANVAS_ID=your-canvas-id-here

# ======================
# llama.cpp Model Configuration (Required)
# ======================

# Path to the llama.cpp model file (GGUF format)
# Example: /path/to/models/bunny-v1.1-4b-q4_k_m.gguf
MODEL_PATH=/path/to/your/model.gguf

# Context size (token window for the model)
# Typical values: 2048, 4096, 8192, 16384
# Higher values use more memory but allow longer conversations
CONTEXT_SIZE=4096

# Batch size for processing tokens
# Typical values: 512, 1024, 2048
# Higher values may improve throughput but use more memory
BATCH_SIZE=512

# Number of layers to offload to GPU
# Set to 0 for CPU-only inference
# Set to -1 to offload all layers to GPU
# Or specify a specific number (e.g., 32, 40) based on your GPU memory
GPU_LAYERS=-1

# Number of simultaneous contexts to support
# Allows handling multiple concurrent requests
# Typical values: 1-4 (depends on available memory)
NUM_CONTEXTS=2

# Temperature for text generation (0.0 - 2.0)
# Lower values (0.1-0.7) are more deterministic
# Higher values (0.8-1.5) are more creative
TEMPERATURE=0.7

# Top-P sampling (0.0 - 1.0)
# Controls diversity via nucleus sampling
# Typical values: 0.9-0.95
TOP_P=0.9

# Repeat penalty (1.0 - 2.0)
# Reduces repetition in generated text
# 1.0 = no penalty, higher values penalize repetition more
REPEAT_PENALTY=1.1

# ======================
# Optional Settings
# ======================

# Allow self-signed SSL certificates (for development only)
# Set to true only for development/testing with self-signed certificates
ALLOW_SELF_SIGNED_CERTS=false

# Web UI password (if web interface is enabled)
WEBUI_PWD=your-webui-password-here

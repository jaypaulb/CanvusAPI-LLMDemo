# =============================================
# CanvusLocalLLM - Minimal Configuration
# =============================================
# This is the minimal configuration for zero-config local AI deployment.
# All AI settings use intelligent defaults for local GPU inference.
#
# Quick Start:
# 1. Copy this file to `.env`
# 2. Configure your Canvus connection (4 required variables below)
# 3. Start the application - models download automatically on first run
#
# For advanced configuration options, see ADVANCED_CONFIG.md
# =============================================

# =============================================
# Canvus Connection (REQUIRED)
# =============================================

# Canvus server URL - Your Canvus instance base URL
# Example: https://canvus.yourcompany.com or https://your-server.example.com
CANVUS_SERVER=https://your-canvus-server.com

# Canvas ID - The UUID of the canvas to monitor
# Find this in your canvas URL or canvas settings
# Example: 123e4567-e89b-12d3-a456-426614174000
CANVAS_ID=your-canvas-id-here

# =============================================
# Canvus Authentication (REQUIRED - choose one method)
# =============================================

# Method 1: API Key (recommended)
CANVUS_API_KEY=your-canvus-api-key-here

# Method 2: Username/Password (if API key not available)
# Uncomment and configure if using username/password authentication:
# CANVUS_USERNAME=your-username
# CANVUS_PASSWORD=your-password

# =============================================
# Web UI Security (REQUIRED)
# =============================================

# Password for accessing the web interface
# Choose a strong password to protect your installation
WEBUI_PWD=your-secure-password-here

# =============================================
# Optional Settings
# =============================================

# Canvas Name (optional) - Human-readable name for logging
# If not set, will be fetched from the API
# CANVAS_NAME=My Canvas

# Port for web interface (optional, default: 3000)
# PORT=3000

# Allow self-signed SSL certificates (optional, default: false)
# WARNING: Only enable for development/testing with self-signed certificates
# Setting this to true disables SSL certificate validation - use with caution
# ALLOW_SELF_SIGNED_CERTS=false

# =============================================
# Local Image Generation (Optional - Stable Diffusion)
# =============================================
# Configure local image generation via Stable Diffusion.
# Requires NVIDIA GPU with CUDA support and stable-diffusion.cpp library.
#
# SD_MODEL_PATH=./models/sd-v1-5.safetensors  # Path to SD model file (required to enable)
# SD_IMAGE_SIZE=512                            # Image dimensions (512, 768, or 1024)
# SD_INFERENCE_STEPS=20                        # Number of denoising steps (1-100)
# SD_GUIDANCE_SCALE=7.5                        # CFG scale (1.0-30.0)
# SD_NEGATIVE_PROMPT=                          # Default negative prompt
# SD_TIMEOUT_SECONDS=120                       # Generation timeout in seconds
# SD_MAX_CONCURRENT=1                          # Max concurrent generations

# =============================================
# Cloud API Fallback (Optional - Advanced)
# =============================================
# By default, all AI processing uses local GPU inference.
# Configure these only if you want cloud API fallback for specific features:
#
# OPENAI_API_KEY=sk-...           # For cloud text/image generation fallback
# GOOGLE_VISION_API_KEY=...       # For handwriting recognition (OCR)
#
# See ADVANCED_CONFIG.md for all available configuration options.
